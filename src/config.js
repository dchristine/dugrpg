Config = Backbone.Model.extend({
    defaults: {
        renderType: 'Canvas',
        env: 'dev',
        gameVersion: '0.0.1',
        scene: 'main'
    },
    initialize: function () {
        
    }
});